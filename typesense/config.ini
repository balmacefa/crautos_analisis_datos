; Typesense Configuration File

[server]
data-dir = /data
api-key = xyz123abc456
enable-cors = true
api-port = 8108
log-dir = /data/logs
; Number of search threads. A value of 0 means auto-detect based on CPU cores.
thread-pool-size = 0

; Production-specific settings
log-level = info
slow-query-log-threshold-ms = 500
